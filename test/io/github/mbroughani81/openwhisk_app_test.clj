(ns io.github.mbroughani81.openwhisk-app-test
  (:require [clojure.test :refer :all]
            [io.github.mbroughani81.openwhisk-app :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
